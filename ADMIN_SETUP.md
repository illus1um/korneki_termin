# 🔐 Настройка админ-панели

## 📋 Быстрый старт

### 1. Добавьте свой Telegram ID в `.env`

```env
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
```

**Как узнать свой Telegram ID:**
- Напишите боту [@userinfobot](https://t.me/userinfobot)
- Или используйте [@getidsbot](https://t.me/getidsbot)
- Скопируйте число из поля "Id"

### 2. Перезапустите бота

```bash
python bot.py
```

### 3. Откройте админ-панель

- Команда: `/admin`
- Или нажмите кнопку **🔐 Админ** в меню категорий (только для админов)

---

## 📊 Функциональность

### ✅ Статистика и аналитика

- **`/admin` → 📊 Статистика**
  - Пользователи (уникальные, активные)
  - Распределение по языкам
  - Топ категорий
  - Статистика поиска (успешные/неуспешные запросы)

- **`/admin` → 🔍 Топ запросы**
  - Топ-10 популярных поисковых запросов
  - Запросы без результатов (что добавить в базу?)

### ✅ Мониторинг и диагностика

- **`/admin` → 💚 Здоровье бота**
  - Количество терминов в памяти
  - Размеры файлов
  - Статус кэшей
  - Производительность

- **`/admin` → ❌ Ошибки**
  - Журнал ошибок за последние 24 часа

### ✅ Экспорт и импорт

- **`/admin` → 📤 Экспорт**
  - Экспорт аналитики (CSV)
  - Экспорт терминов (CSV)

### ✅ Бэкап и восстановление

- **`/admin` → 💾 Бэкапы**
  - Создание бэкапа CSV файла
  - Список всех бэкапов
  - Автоматическое сохранение в `data/backups/`

### ✅ Настройки

- **`/admin` → ⚙️ Настройки**
  - Текущие настройки бота

---

## 📁 Структура файлов

```
data/
  ├── extracted_terms_full.csv    # Основная база терминов
  ├── analytics.csv                # Логи событий (создаётся автоматически)
  └── backups/                     # Папка для бэкапов
      └── backup_YYYYMMDD_HHMMSS.csv
```

---

## 📝 Логирование событий

Система автоматически логирует:

- ✅ Выбор языка (`language_selected`)
- ✅ Выбор категории (`category_selected`)
- ✅ Выбор подкатегории (`subcategory_selected`)
- ✅ Поисковые запросы (`search`)

Все события сохраняются в `data/analytics.csv`

---

## 🔒 Безопасность

- Только пользователи из `ADMIN_IDS` имеют доступ к админке
- Все команды проверяют права доступа
- Бэкапы создаются автоматически перед критическими операциями

---

## 🚀 Использование

1. **Просмотр статистики:**
   ```
   /admin → 📊 Статистика → За 7 дней
   ```

2. **Создание бэкапа:**
   ```
   /admin → 💾 Бэкапы → 💾 Создать бэкап
   ```

3. **Экспорт данных:**
   ```
   /admin → 📤 Экспорт → 📊 Экспорт аналитики
   ```

---

## 📊 Пример статистики

```
📊 Статистика за 7 дней

👥 Пользователи:
  • Всего уникальных: 234
  • Активных сегодня: 45
  • Событий сегодня: 156

🌐 Языки:
  • Казахский: 152 (65.0%)
  • Русский: 82 (35.0%)

📂 Топ-5 категорий:
  1. Медицина: 45
  2. Автосервис: 32
  3. Туризм: 28
  ...

🔍 Поиск:
  • Всего запросов: 456
  • Успешных: 389
  • Без результатов: 67
  • Успешность: 85.3%
```

---

## ⚠️ Важно

- Админка доступна только пользователям из `ADMIN_IDS`
- Бэкапы сохраняются в `data/backups/`
- Аналитика записывается в `data/analytics.csv` автоматически
- Все экспорты отправляются как файлы в Telegram

