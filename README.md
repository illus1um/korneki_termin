# 🤖 Telegram-бот для поиска терминов

Telegram-бот на Python с использованием aiogram 3 для поиска терминов в базе данных CSV.

## 📁 Структура проекта

```
korneki/
│
├── bot.py                      # Точка входа, запуск бота
├── config.py                   # Конфигурация (загрузка настроек из .env)
├── requirements.txt            # Зависимости проекта
├── .env                        # Файл с переменными окружения (создайте сами)
│
├── data/
│   └── terms_sample.csv        # База данных терминов
│
├── handlers/                   # Обработчики команд и сообщений
│   ├── __init__.py
│   ├── start.py                # Обработчик команды /start
│   └── terms.py                # Обработчик поиска терминов
│
├── services/                   # Бизнес-логика
│   ├── __init__.py
│   └── terms_service.py        # Сервис для работы с CSV
│
└── utils/                      # Вспомогательные функции
    ├── __init__.py
    └── formatter.py            # Форматирование результатов
```

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий (или скачайте проект)

```bash
git clone <your-repo-url>
cd korneki
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте файл `.env`

Создайте файл `.env` в корне проекта и добавьте токен вашего бота:

```env
BOT_TOKEN=your_bot_token_here
```

> 💡 Получить токен можно у [@BotFather](https://t.me/BotFather) в Telegram

⚠️ **Важно:** Файл `.env` содержит секретные данные и автоматически исключен из Git через `.gitignore`

### 4. Запустите бота

```bash
python bot.py
```

## 🔍 Функционал

- **Команда `/start`** — приветственное сообщение
- **Текстовые сообщения** — поиск терминов в базе данных
  - Точный поиск (полное совпадение)
  - Частичный поиск (по вхождению)
  - Возвращает максимум 5 результатов

## 📊 Формат данных CSV

Файл `data/terms_sample.csv` должен содержать следующие колонки:

```
term,description,category,subcategory,lang
```

Пример:
```
Салауат,Денсаулық пен амандықты білдіретін сөз.,Медицина,Тіс емханасы,kk
```

## 🛠 Технологии

- **Python 3.10+**
- **aiogram 3.13.1** — асинхронный фреймворк для Telegram Bot API
- **pydantic-settings 2.5.2** — управление конфигурацией
- **python-dotenv 1.0.1** — загрузка переменных окружения

## 📝 Пример использования

1. Отправьте боту `/start` для приветствия
2. Отправьте любой текст, например: `Салауат`
3. Бот вернет найденные термины с описанием, категорией и языком

Формат ответа:
```
🔍 Найдено результатов: 2

1. **Салауат** _(Медицина / Емхана / kk)_
Денсаулық пен ұзақ ғұмырдың белгісі.

2. **Салауат** _(Медицина / Тіс емханасы / kk)_
Денсаулық пен амандықты білдіретін сөз.
```

## 🔐 Безопасность

Файл `.env` содержит конфиденциальные данные (токен бота) и **не должен** попадать в Git.

Убедитесь, что:
- ✅ Файл `.gitignore` содержит `.env`
- ✅ Перед первым коммитом проверьте, что `.env` не отслеживается

```bash
# Проверить неотслеживаемые файлы
git status
```

## 📄 Лицензия

MIT

